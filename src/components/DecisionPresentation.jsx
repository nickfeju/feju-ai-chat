export function DecisionPresentation({data}){const d=data?.data||data;if(!d)return null;const forecast=d.type==="executive-forecast"||d.schemaVersion==="4.4";const scenario=d.type==="executive-scenario"||d.scenario;if(!forecast&&!scenario)return null;const items=Array.isArray(d.forecasts)?d.forecasts:Array.isArray(d.results)?d.results:[];return <section className="decision-presentation"><h4>{scenario?"Scenariovergelijking":"Voorspellende vooruitblik"}</h4>{items.map((x,i)=><div className="decision-row" key={x.name||x.metric||i}><span>{x.name||x.metric||`Uitkomst ${i+1}`}</span><strong>{x.displayValue||x.value||x.projectedValue||"—"}</strong>{x.changePercent!=null&&<small>{x.changePercent>0?"+":""}{x.changePercent}%</small>}</div>)}</section>}
