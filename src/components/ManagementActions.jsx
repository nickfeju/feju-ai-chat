export function ManagementActions({message,onSelect}){const signals=message?.data?.signals||message?.raw?.data?.signals||[];if(!signals.length||!onSelect)return null;const top=signals[0];return <div className="management-actions"><button onClick={()=>onSelect({mode:"auto",prompt:`Geef een verdiepend actieplan voor ${top.companyName}`})}>Verdiep topprioriteit</button><button onClick={()=>onSelect({mode:"auto",prompt:"Wat is er veranderd sinds de vorige analyse?"})}>Ververs veranderingen</button><button onClick={()=>onSelect({mode:"auto",prompt:"Maak hiervan een executive briefing met eigenaren en deadlines"})}>Maak briefing</button></div>}
